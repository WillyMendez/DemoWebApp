version: "1"
project:
  name: DemoWebApp
  branch: master
  revision:
    name: ${scm.git.commit}
    date: "2021-07-08T17:24:02-06:00"
capture:
  build:
    cleanCommands:
    - shell: [mvn, -B, -f, pom.xml, clean]
    buildCommands:
    - shell: [mvn, -B, -f, pom.xml, install]
  fileSystem:
    ears:
      extensions: [ear]
      files:
      - directory: /var/jenkins_home/workspace/Demos/DemoWebApp/
    java:
      files:
      - directory: /var/jenkins_home/workspace/Demos/DemoWebApp/src/main
    javascript:
      files:
      - directory: .
      - excludeRegex: node_modules|bower_components|vendor
    php:
      files:
      - directory: .
    python:
      files:
      - directory: .
    ruby:
      files:
      - directory: .
    wars:
      extensions: [war]
      files:
      - directory: .
analyze:
  mode: central
install:
  coverity:
    version: default
serverUrl: https://poc179.polaris.synopsys.com

